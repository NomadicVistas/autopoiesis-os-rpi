# Autopoiesis OS + Frames Program

Program tag: autopoiesis_os_frames

Database namespace: aos

Table prefix: aos_

This directory keeps the major build program separate from ordinary repo docs while still traveling with the GitHub repository.

## Scope

First target: Raspberry Pi touchscreen display appliance.

Future targets: desktop, phone, tablet, and other ambient display devices.

## Priority Chain

profile -> database -> API -> pairing -> sync -> kiosk -> feed -> cache -> broadcast -> updates -> admin -> production rollout

